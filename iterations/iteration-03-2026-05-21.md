# Iteration 3 - 2026-05-21

## 上一轮错误修复
- [x] 错误1：TypeScript 类型导入未使用 `type-only import` → 修复方案：将所有类型导入改为 `import type { ... }` → 验证结果：构建通过
- [x] 错误2：项目未安装 `react-router-dom` 但代码中使用了 `useParams`/`useNavigate` → 修复方案：改为使用原生 `window.location.pathname` 解析路由 → 验证结果：构建通过
- [x] 错误3：`VerticalText` 组件中存在未使用的 `variant` 变量 → 修复方案：移除该变量 → 验证结果：构建通过

## 本轮优化方向

### 已完成功能
1. **古籍数字化阅览系统核心实现**
   - 创建了完整的类型定义 (`src/types/classical.ts`)
   - 创建了《山海经》古籍数据 (`src/data/classicalTexts.ts`)，包含南山经、西山经、北山经三章
   - 实现了竖排文字组件 (`VerticalText.tsx`)，支持 `writing-mode: vertical-rl`
   - 实现了注释弹窗组件 (`AnnotationPopup.tsx`)，点击标注词汇显示释义
   - 实现了章节导航组件 (`ChapterNav.tsx`)，支持上一章/下一章切换
   - 实现了外部链接组件 (`ExternalLinks.tsx`)，集成识典古籍、国家图书馆等平台
   - 实现了主阅读器组件 (`ClassicalReader.tsx`)，整合所有功能

2. **路由集成**
   - 在 `App.tsx` 中添加了 `/reader/:textId` 路由支持
   - 使用 `React.lazy` + `Suspense` 实现代码分割，阅读器组件单独打包 (20.68KB gzip)

3. **资源查询页面增强**
   - 在 `ResourceQuery.tsx` 中为《山海经》卡片添加了"在线阅读"按钮
   - 点击按钮导航至 `/reader/shanhaijing`

4. **样式系统**
   - 在 `index.css` 中添加了古籍阅读器主题变量
   - 添加了竖排文字容器样式、标注词汇样式、滚动条样式

### 技术债务
- [ ] 当前使用原生路由而非 React Router，如需更复杂的路由功能建议引入 `react-router-dom`
- [ ] 古籍数据目前为硬编码，后续可考虑从本地 JSON 文件或 API 加载
- [ ] 竖排文字的标注词汇定位采用基于字符位置的简单算法，对于复杂排版可能需要优化
- [ ] 缺少繁简转换的实时计算，目前采用预存两套文本的方式

## 验证结果
- [x] TypeScript 编译：通过
- [x] Vite 构建：通过
- [x] 代码分割：`ClassicalReader` 组件独立 chunk (20.68KB gzip)
- [x] 总构建时间：13.14s
